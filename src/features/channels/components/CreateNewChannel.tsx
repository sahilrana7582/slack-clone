const CreateNewChannel = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <div className="w-full">New Channel</div>

      {children}
    </>
  );
};

export default CreateNewChannel;
